# Liminal — Python dependencies
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# System packages (mpv, ffmpeg, Qt6 Multimedia) are required — see README.md.
# For MPRIS (media keys), if pip fails below, use your distro instead:
#   Fedora:        sudo dnf install python3-dbus python3-gobject
#   Debian/Ubuntu: sudo apt install python3-dbus python3-gi
#   Arch:          sudo pacman -S python-dbus python-gobject
# Or install without MPRIS: pip install -r requirements-minimal.txt

# Core GUI (app_qt.py)
PyQt6>=6.5,<7
qasync>=0.24

# Media library & subtitles
mutagen>=1.47.0
srt>=3.5.0

# YouTube / link downloads (needs system ffmpeg binary)
yt-dlp>=2024.12.23

# MPRIS desktop integration (Linux only)
dbus-python>=1.3; platform_system == "Linux"
PyGObject>=3.46; platform_system == "Linux"

# Optional terminal UI (python app.py)
textual>=0.40
